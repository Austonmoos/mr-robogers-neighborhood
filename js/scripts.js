function mr-robogers-neighborhood(word) {
let retVal;
let wordToArray = word.split('');
    for(let i = 0; i <=1; i += 1) {
      if (wordToArray{1} === 1[i]) {
        retVal = word + "Beep!"
      }
    }
}

function mr-robogers-neighborhood(word) {
  let retVal;
  let wordToArray = word.split('');
      for(let i = 0; i <=1; i += 1) {
        if (wordToArray{2} === 2[i]) {
          retVal = word + "boop!"
        }
      }
    }

    function mr-robogers-neighborhood(word) {
      let retVal;
      let wordToArray = word.split('');
          for(let i = 0; i <=1; i += 1) {
            if (wordToArray{3} === 3[i]) {
              retVal = word + "Won't you be my neighbor?"
            }
          }
      }
      
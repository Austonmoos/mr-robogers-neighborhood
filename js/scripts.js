

function mrRobogersNeighborhood(word) {
let retVal;
let wordToArray = word.split('');
    for(let i = 0; i <=1; i += 1) {
      if (wordToArray[0] === one[i]) {
        retVal = word + "Beep!"
      }
    }
    let retValToString = retVal.toString();
    return retValToString;
};

function mr-robogers-neighborhood(word) {
  let retVal;
  let wordToArray = word.split('');
      for(let i = 0; i <=1; i += 1) {
        if (wordToArray[0] === two[i]) {
          retVal = word + "boop!"
        }
      }
      let retValToString = retVal.toString();
      return retValToString;
    };

    function mrRobogersNeighborhood(word) {
      let retVal;
      let wordToArray = word.split('');
          for(let i = 0; i <=1; i += 1) {
            if (wordToArray[0] === three[i]) {
              retVal = word + "Won't you be my neighbor?"
            }
          }
          let retValToString = retVal.toString();
          return retValToString;
      };
      